import pyautogui

scWidth, scHeight = pyautogui.size()    #Fetches monitor resolution
scWidth, scHeight (1920,1080)   #Default monitor size

cursorX, cursorY = pyautogui.position()    #Fetches XY coordinate of cursor
cursorX, cursorY (1314,345)     #Default cursor position

